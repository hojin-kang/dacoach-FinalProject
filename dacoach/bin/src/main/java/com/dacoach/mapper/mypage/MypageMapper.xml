<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dacoach.mapper.mypage.MypageMapper">

	<select id="getUserInfo" resultType="map">
		<![CDATA[
			SELECT
			    C.NICKNAME  AS nickname,
			    R.RANK_NAME AS rankname
			FROM COACH C
			LEFT JOIN RANK R
			  ON NVL(C.POINT_SCORE, 0) >= R.MIN_SCORE
			 AND (R.MAX_SCORE IS NULL OR NVL(C.POINT_SCORE, 0) < R.MAX_SCORE)
			WHERE C.USERS_IDX = #{users_idx}
		]]>
	</select>
</mapper>