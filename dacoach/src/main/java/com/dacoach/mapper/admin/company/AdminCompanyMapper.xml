<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dacoach.mapper.admin.company.AdminCompanyMapper">

  <select id="companyList" resultType="com.dacoach.model.company.CompanyDTO">
    SELECT 
      u.users_idx     AS usersIdx,
      u.user_name     AS userName,
      c.company_num   AS companyNum,
      ce.cert_status  AS certStatus
    FROM users u
    JOIN company c ON u.users_idx = c.users_idx
    LEFT JOIN cert ce ON u.users_idx = ce.users_idx
    WHERE u.user_type = 'COMPANY'
  </select>

</mapper>
