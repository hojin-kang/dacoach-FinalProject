<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.dacoach.mapper.company.CompanyMapper">
  	<select id="fieldTag">
  		select * from major_field order by major_field_idx desc
  	</select>
  	
  	<select id="regionTag">
  		select * from major_region order by major_region_idx desc
  	</select>
  </mapper>